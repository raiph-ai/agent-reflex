---
name: agent-reflex-router
description: "Use when routing incoming messages/tasks to an agent, project, intent, skill, or toolset."
version: 0.1.0
author: Red Circle Studio
---

# Agent Reflex Router

Run:

```bash
agent-reflex route --input /path/to/message.json
agent-reflex skill --input /path/to/message.json
```

Use the result as guidance, not gospel. Low confidence means the orchestrator should decide.
